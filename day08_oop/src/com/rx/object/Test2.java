package com.rx.object;

public class Test2 {
    //类中定义的变量称为成员变量（对象的属性），类中定义的方法属于成员方法（对象的行为）
    //成员变量本身存在默认值（同数组的初始默认值）
    //一个代码文件中，可以有很多类，但只能存在一个用public修饰的类（该类与代码文件名一致）
    //如果某个对象没有任何变量引用（指向）它，那该对象无法被操作（单独存放在堆内存中），称垃圾对象
}
